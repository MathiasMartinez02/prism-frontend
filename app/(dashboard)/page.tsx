// Placeholder de la lista de repos/PRs, con los tokens del design canvas (direccion "Terminal Dark"). Se conecta al backend real en el bloque 1.4.
export default function DashboardPage() {
  return (
    <main className="min-h-full bg-background p-8">
      <h1 className="font-mono text-2xl font-semibold text-foreground">PRISM</h1>
      <p className="text-sm text-muted">Lista de PRs (pendiente de conectar al backend).</p>
    </main>
  );
}
