// Placeholder de la lista de repos/PRs. Se conecta al backend real en el bloque 1.4.
export default function DashboardPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">PRISM</h1>
      <p className="text-sm text-gray-500">Lista de PRs (pendiente de conectar al backend).</p>
    </main>
  );
}
